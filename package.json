{
    "name": "redmine_jenkins_job",
    "scripts": {
        "check": "biome check --write",
        "format": "biome format --write",
        "lint": "biome lint --write"
    },
    "devDependencies": {
        "@biomejs/biome": "2.3.13",
        "@playwright/test": "^1.58.1",
        "@types/node": "^26.1.2"
    }
}
